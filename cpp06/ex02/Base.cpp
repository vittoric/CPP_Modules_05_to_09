#include "Base.hpp"
#include <iostream> // For potential debug messages, though not strictly required by snippet

// Base destructor definition
Base::~Base() {
    // As per the prompt, this can be empty or have a print statement.
    // For now, let's keep it empty to match the snippet's optional nature.
    // std::cout << "Base destructor called" << std::endl;
}
